# 开发手册

本文档面向本仓库的维护者与贡献者，帮助你快速完成环境搭建、理解项目结构，并按统一规范参与开发、测试与发布。

## 1. 项目简介

`claude-agent-sdk-go` 是一个用于集成 Claude Code CLI 的非官方 Go SDK，提供两套核心 API：

- `Query()`：一次性调用，适合自动化、脚本和 CI/CD 场景
- `Client`：流式交互，适合多轮对话、复杂工作流和实时响应

项目目标是保持与 Python SDK 的高功能对齐，同时遵循 Go 的接口设计、错误处理与上下文管理习惯。

## 2. 适用对象

这份手册适合以下读者：

- 初次进入仓库、需要快速搭建开发环境的同学
- 需要新增功能、修复缺陷或补充测试的贡献者
- 需要理解模块分层、公共 API 与内部实现关系的维护者
- 需要使用 AI 助手在本仓库中协作开发的工程师

## 3. 开发环境要求

### 3.1 基础依赖

- Go 1.18+
- Node.js
- Claude Code CLI：`npm install -g @anthropic-ai/claude-code`

### 3.2 建议校验

```bash
go version
node -v
claude --version
```

如果只是进行静态开发、补文档或补测试，Claude Code CLI 不是所有任务的硬前置；但涉及真实集成验证时必须可用。

## 4. 快速开始

### 4.1 获取代码

```bash
git clone https://github.com/tea4go/claude-agent-sdk-go.git
cd claude-agent-sdk-go
```

### 4.2 基础验证

```bash
go build ./...
go test ./...
```

### 4.3 常用命令

```bash
# 格式化与静态检查
gofmt -s -w .
go vet ./...
golangci-lint run
gocyclo -over 15 .

# 推荐的一站式检查
make check

# 基准测试
make bench

# 本地完整 CI 流程
make ci
```

## 5. 仓库结构总览

```text
.
├── client.go              # Client 接口与连接生命周期管理
├── query.go               # Query API（一次性调用）
├── options.go             # Options 与函数式选项
├── types.go               # 对外导出的类型与常量
├── errors.go              # 结构化错误类型
├── session.go             # 会话管理能力
├── mcp.go                 # MCP 相关公开 API
├── skills.go              # Skills 相关能力
├── slash_commands.go      # Slash commands 相关能力
├── internal/
│   ├── cli/               # CLI 发现与命令构建
│   ├── control/           # 控制协议（权限、Hook、MCP）
│   ├── parser/            # JSON 消息解析
│   ├── shared/            # 共享类型与公共逻辑
│   ├── session/           # 会话存储与查询
│   └── subprocess/        # 子进程管理与协议适配
├── examples/              # 编号示例，按复杂度递进
├── docs/                  # 架构、参考文档、追踪文档
├── README.md              # 英文入口文档
├── README.zh-CN.md        # 中文入口文档
├── CONTRIBUTING.md        # 贡献指南
├── ARCHITECTURE.md        # 架构总览
├── AGENTS.md              # 面向 Codex 的仓库协作说明
└── CLAUDE.md              # 面向 Claude Code 的仓库协作说明
```

## 6. 核心架构理解

### 6.1 数据流

项目整体的数据流如下：

1. 调用方通过 `Query()` 或 `Client` 发起请求
2. 请求经由 `Transport` 抽象进入 `internal/subprocess`
3. 子进程与 Claude Code CLI 通过标准输入输出通信
4. CLI 输出由 `internal/parser` 解析为消息对象
5. 上层 API 将 `AssistantMessage`、`ResultMessage` 等类型暴露给调用方
6. 若涉及权限、Hook、MCP 等控制逻辑，则通过 `internal/control` 协议完成协商

### 6.2 设计原则

- **接口驱动：** 核心组件通过接口解耦，便于测试和扩展
- **Context 优先：** 所有阻塞操作都接收 `context.Context`
- **函数式选项：** 通过 `WithXxx()` 组合配置
- **结构化错误：** 对常见错误类型提供 `Is*` / `As*` 辅助函数
- **公开 API 与内部实现分层明确：** 根包负责对外能力，`internal/` 负责具体实现

## 7. 两套 API 的使用边界

### 7.1 何时使用 `Query()`

适合以下场景：

- 一次性提问或单步自动化
- CI/CD 中的固定任务
- 与历史上下文无关的独立调用
- 希望自动管理连接与资源的简单脚本

### 7.2 何时使用 `Client`

适合以下场景：

- 需要多轮上下文
- 需要流式响应
- 需要运行时切换模型或权限模式
- 需要使用 Hook、文件检查点、MCP 状态查询等高级能力

### 7.3 推荐写法

优先使用 `WithClient()` 包裹 `Client` 生命周期，而不是手动 `Connect()` / `Disconnect()`。这能减少资源泄漏风险，也更符合 Go 的使用习惯。

## 8. 开发工作流建议

### 8.1 修改前先定位影响范围

开始改动前，优先确认：

- 变更属于根包公开 API，还是 `internal` 内部实现
- 是否会影响现有示例、测试或文档
- 是否涉及 Python SDK 对齐行为
- 是否会改变 wire format、错误类型或控制协议行为

### 8.2 推荐开发步骤

1. 阅读相关实现和现有测试
2. 明确行为变更点
3. 先补测试或先构造失败用例
4. 实现修改
5. 运行格式化与校验
6. 更新示例或文档（如有必要）

### 8.3 提交前最少检查

```bash
gofmt -s -w .
go vet ./...
go test ./...
```

若变更影响较大，建议继续执行：

```bash
make check
make ci
```

## 9. 代码规范

### 9.1 Go 风格

- 保持 `gofmt` 输出为准
- 使用清晰、具体的命名
- 减少不必要导出
- 错误包装使用 `fmt.Errorf("xxx: %w", err)`

### 9.2 圈复杂度

- 目标阈值：15
- 超限时优先拆分辅助函数
- 表驱动测试、示例和编排代码可适度放宽，但要有明确理由

### 9.3 配置风格

所有配置优先通过函数式选项暴露，例如：

- `WithSystemPrompt()`
- `WithAllowedTools()`
- `WithPermissionMode()`
- `WithModel()`

### 9.4 注释约定

本项目已统一补齐中文注释，要求包括：

- 结构体注释
- 函数注释
- 重要逻辑的关键注释

新增代码时请延续这个约定，不要引入新的纯英文注释风格。

## 10. 测试规范

### 10.1 常用测试命令

```bash
go test ./...
go test -v ./...
go test -race ./...
go test -cover ./...
```

### 10.2 测试组织方式

推荐顺序：

1. 测试函数
2. Mock 类型
3. 辅助函数

### 10.3 表驱动测试

对于输入输出映射明显、分支较多的逻辑，优先使用表驱动测试。

### 10.4 Mock 要求

- 必须线程安全
- 共享状态要加锁
- 帮助函数中必须调用 `t.Helper()`

### 10.5 已知环境差异

在 Windows 环境下，`internal/subprocess` 的部分测试可能因系统命令差异失败，例如 `echo` 或进程中断相关行为。遇到这类问题时，需要先区分：

- 是仓库原有的跨平台测试差异
- 还是当前改动真的引入了新问题

不要仅凭一次失败就直接认定逻辑错误。

## 11. 基准测试与性能

常用命令：

```bash
make bench
go test -bench=. -benchmem ./...
```

编写 benchmark 时请注意：

- 使用 `var sink any` 防止编译器消除死代码
- 使用 `b.ReportAllocs()` 统计分配
- 使用 `b.ResetTimer()` 排除初始化开销

## 12. 示例目录使用方式

`examples/` 是理解 SDK 最快的入口，建议按编号学习：

- `01` - `03`：基础 Query / Client 用法
- `04` - `07`：工具与 MCP 集成
- `08` - `14`：高级工作流、权限、Hook、检查点、SDK MCP Server
- `15` - `24`：子 agent、结构化输出、插件、沙箱、调试、技能注册、斜杠命令

当你新增或修改公开能力时，优先思考：

- 是否已有示例需要同步更新
- 是否应该新增一个更贴近真实场景的示例

## 13. 文档体系

建议按以下顺序阅读：

1. [README.zh-CN.md](README.zh-CN.md)：项目总览与快速开始
2. [CONTRIBUTING.md](CONTRIBUTING.md)：贡献方式与基本规范
3. [ARCHITECTURE.md](ARCHITECTURE.md)：架构入口
4. [docs/reference.md](docs/reference.md)：公开 API 参考
5. `docs/architecture/`：更细的组件、数据流、接口与设计模式说明
6. `docs/tracking/README.md`：Python SDK 对齐跟踪

如果你新增功能但没有更新对应文档，这通常意味着工作还没有真正收尾。

## 14. AI 协作相关文件

仓库中有两份专门给 AI 助手使用的说明文件：

- `AGENTS.md`：面向 Codex
- `CLAUDE.md`：面向 Claude Code

这两份文档现在都已经转为中文，主要作用是：

- 说明仓库结构与常用命令
- 固化代码约定、测试约定与架构背景
- 记录项目级模式和近期关注点

如果你依赖 AI 助手协作开发，建议保持这两份文件与实际仓库状态同步更新。

## 15. 提交与 PR 规范

### 15.1 提交信息

采用约定式提交：

- `feat:`
- `fix:`
- `docs:`
- `test:`
- `refactor:`
- `chore:`

### 15.2 PR 前检查清单

- [ ] 代码已格式化
- [ ] 测试已运行
- [ ] 文档已同步
- [ ] 示例已同步（如果影响公开能力）
- [ ] 提交信息清晰

### 15.3 分支建议

```bash
git checkout main
git pull origin main
git checkout -b feature/issue-N-short-description
```

## 16. 常见任务速查

### 16.1 只做文档修改

```bash
git diff -- README.md README.zh-CN.md 开发手册.md
```

### 16.2 只验证某类测试

```bash
go test -v -run TestClient
go test -count=3 -run TestClient
```

### 16.3 查看公开 API

```bash
go doc .
go doc Query
go doc Client
```

### 16.4 运行发布前检查

```bash
make release-check
```

## 17. 故障排查

### 17.1 `go test ./...` 失败

先判断失败类型：

- 编译错误：通常是代码改动引入
- 断言失败：通常是行为变更未同步测试
- 环境失败：可能是 CLI、Node.js、系统命令或平台差异导致

### 17.2 CLI 不可用

若出现 CLI 未找到或连接失败：

```bash
npm install -g @anthropic-ai/claude-code
```

并检查可执行文件是否在 `PATH` 中。

### 17.3 公开 API 变更后行为异常

优先检查：

- 根包公开类型是否和 `internal` 实现保持一致
- 示例代码是否还反映当前行为
- 文档是否描述了旧接口或旧语义

## 18. 开发建议

- 小步提交，避免一次改动过大
- 先看现有测试，再动核心协议与消息类型
- 影响公开 API 时，测试、示例、文档要一起改
- 对控制协议、消息字段、MCP 行为的修改要格外谨慎
- 不要忽略 `AGENTS.md` / `CLAUDE.md` 里的项目级约束

## 19. 维护建议

建议在以下情况下同步更新本手册：

- 仓库结构发生明显变化
- 开发命令或 Makefile 目标变更
- 测试策略或发布流程变化
- 新增重要公开 API 或重要示例
- AI 协作方式发生变化

---

如果你是第一次参与本项目，建议先完成以下路线：

1. 阅读 `README.zh-CN.md`
2. 阅读 `ARCHITECTURE.md`
3. 跑通 `examples/01_quickstart`
4. 跑通 `go test ./...`
5. 再开始改动根包公开 API 或 `internal/` 核心实现
